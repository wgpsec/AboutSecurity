# 快速生成所需要规则的字典

比如，批量这个文件夹里的文件弄到一个文件里，然后用正则过滤

```
cat *.txt > output.txt
```

只有数字和小写字母的组合 至少8位字符
```re
^(?![0-9]+$)(?![a-z]+$)[0-9a-z]{8,16}$
```

只有数字和大小写字母的组合 至少8位字符
```re
^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]{8,16}$
```

参考: https://github.com/r35tart/RW_Password
